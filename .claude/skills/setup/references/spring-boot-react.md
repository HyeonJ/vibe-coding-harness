# Spring Boot + React 부트스트랩

> ⚠️ 골격 단계 — 본문은 v0.1.0 빌드 시 채워질 예정.

## 채울 항목 (TODO)
- [ ] 모노레포 vs 분리 결정
  - 사내 표준 권장: `backend/` + `frontend/` 단일 레포 분리 폴더
- [ ] backend: Spring Initializr (Thymeleaf 제외)
- [ ] frontend: Vite + React + TypeScript
- [ ] CORS 설정 (개발 시 localhost 허용)
- [ ] API 프록시 설정 (Vite proxy 또는 dev profile)
- [ ] 빌드 통합 (Spring 빌드 시 frontend도 같이? 또는 분리 배포?)
- [ ] `.gitattributes`, `.gitignore` (양쪽 모두)
- [ ] Docker Compose (백 + 프론트 + DB 한 번에)
- [ ] README (양쪽 실행 가이드)
- [ ] `_workspace/` 컨벤션
