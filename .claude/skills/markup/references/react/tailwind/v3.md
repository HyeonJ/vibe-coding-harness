# React + Tailwind v3 전용 가이드

> ⚠️ 골격 단계 — 첫 v3 프로젝트 시도 시 채울 예정.

`react/_common.md` + `tailwind/_common.md` 와 함께 로드. 이 파일은 **Tailwind v3.x** 프로젝트에만 해당. 프로젝트 `package.json` 의 `tailwindcss` 가 `^3.x` 일 때만 참조.

v4 프로젝트라면 이 파일 **읽지 말고** `v4.md` 참조.

## 채울 항목 (TODO)
- [ ] 셋업 파일 위치 (`tailwind.config.ts`, `postcss.config.js`, CSS 진입점)
- [ ] 토큰 정의 패턴 (CSS 변수 → `theme.extend` 매핑)
- [ ] className 작동 방식 (JIT, theme.extend 등록 vs arbitrary value)
- [ ] content 배열 (purge 동작)
- [ ] publisher 에이전트 주의 사항

## 공식 문서
- https://v3.tailwindcss.com/docs/configuration
