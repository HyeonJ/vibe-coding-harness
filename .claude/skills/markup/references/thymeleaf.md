# Thymeleaf 마크업 가이드

> ⚠️ 골격 단계 — 본문은 v0.1.0 빌드 시 채워질 예정.

## 채울 항목 (TODO)
- [ ] 템플릿 디렉토리 구조 (`templates/`, `templates/fragments/`, `templates/layouts/`)
- [ ] Fragment 패턴 (`th:fragment`, `th:replace`, `th:insert`)
- [ ] 레이아웃 시스템 (Thymeleaf Layout Dialect)
- [ ] 속성 순서 컨벤션 (id → class → th:* → data-* → 이벤트)
- [ ] CSS 구조 (`static/css/`, BEM 또는 케밥케이스)
- [ ] 디자인 토큰 → CSS 변수 매핑
- [ ] Boolean 속성: `th:disabled="${condition}"`
- [ ] i18n (`th:text="#{key}"`)
- [ ] Figma → Thymeleaf 컴포넌트 변환 패턴
- [ ] 반응형 (Bootstrap 5 또는 사내 그리드)
- [ ] 접근성 (a11y) 기본 (aria-*, role)

## 참조 (글로벌 CLAUDE.md)
- HTML 속성 순서, fragment 분리, Boolean 속성 형식
- CSS: `!important` 금지, 셀렉터 깊이 3단계 이하
